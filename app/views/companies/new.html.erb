<%= form_for @company do |f| %>
  <div class="form-group">
    <%= f.label :company_name %>
    <span>
      <%= f.text_field :company_name, :class => "form-control", :placeholder => "Company" %>
    </span>
  </div>
  <%= submit_tag "Submit", class: "btn btn-default" %>
<% end %>